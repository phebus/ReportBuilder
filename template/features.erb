<ul class="featureList collapsible popout" data-collapsible="expandable">
  <% features.each_with_index do |feature, f| %>
      <% fid = "#{gid}f#{f}" %>
      <%= template('feature').result(binding) %>
  <% end %>
</ul>
